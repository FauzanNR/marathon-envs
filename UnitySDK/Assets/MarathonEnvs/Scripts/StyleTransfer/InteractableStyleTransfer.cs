using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteractableStyleTransfer : MonoBehaviour
{


    // var actionDirection = Vector3.zero;
    // actionDirection.x = vectorAction[vectorAction.Count() - 1];
    //     actionDirection.z = vectorAction[vectorAction.Count() - 2];
    //     torsoRb.AddForce(actionDirection* 10);
}
